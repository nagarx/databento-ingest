# Databento credentials — copy to credentials.toml and fill in.
# credentials.toml is gitignored; NEVER commit actual credentials.

[databento]
api_key = "db-YOUR_API_KEY"  # Required: get from https://databento.com/portal/keys
